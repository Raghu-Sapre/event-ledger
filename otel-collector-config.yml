receivers:
  otlp:
    protocols:
      http:

exporters:
  logging:
    loglevel: info

service:
  pipelines:
    metrics:
      receivers: [ otlp ]
      exporters: [ logging ]
    traces:
      receivers: [ otlp ]
      exporters: [ logging ]
